---
import { Icon } from 'astro-icon/components';
import { skills } from '@/data/site';
import { getSkillIcon } from '@/lib/getSkillIcon';

const categories = Object.keys(skills) as Array<keyof typeof skills>;
const firstCategory = categories[0];
---

<section id="skills" class="bg-gray-50 py-24 dark:bg-gray-900">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-16 text-center">
      <h2 class="mb-4 text-4xl font-bold text-gray-900 dark:text-gray-100 md:text-5xl">
        Skills &amp; Technologies
      </h2>
      <p class="mx-auto max-w-2xl text-lg text-gray-600 dark:text-gray-400">
        A comprehensive overview of my technical expertise and the technologies I work with.
      </p>
    </div>

    <!-- Category tabs -->
    <div
      id="skills-tablist"
      role="tablist"
      aria-label="Skill categories"
      class="mb-12 flex flex-wrap justify-center gap-3"
    >
      {
        categories.map((category) => {
          const isActive = category === firstCategory;
          return (
            <button
              role="tab"
              id={`tab-${category}`}
              aria-selected={isActive ? 'true' : 'false'}
              aria-controls={`panel-${category}`}
              data-category={category}
              class:list={[
                'chip-tab rounded-full px-6 py-3 text-sm font-medium transition-all duration-300',
                isActive
                  ? 'bg-sky-500 text-white shadow-lg dark:bg-sky-600'
                  : 'bg-gray-100 text-gray-700 hover:bg-gray-200 dark:bg-gray-800 dark:text-gray-300 dark:hover:bg-gray-700',
              ]}
            >
              {category}
              <span
                class:list={[
                  'ml-2 rounded-full px-2 py-0.5 text-xs',
                  isActive
                    ? 'bg-white/20 text-white'
                    : 'bg-gray-200 text-gray-600 dark:bg-gray-700 dark:text-gray-400',
                ]}
              >
                {skills[category].length}
              </span>
            </button>
          );
        })
      }
    </div>

    <!-- Skill panels — all rendered in DOM, toggled via JS -->
    {
      categories.map((category) => {
        const isActive = category === firstCategory;
        return (
          <div
            id={`panel-${category}`}
            role="tabpanel"
            aria-labelledby={`tab-${category}`}
            data-category={category}
            class:list={['skills-panel', isActive ? '' : 'hidden']}
          >
            <div class="grid grid-cols-3 gap-6 sm:grid-cols-4 md:grid-cols-5 lg:grid-cols-6">
              {skills[category].map((skill) => (
                <div class="group flex flex-col items-center">
                  <div class="mb-3 flex h-16 w-16 items-center justify-center rounded-2xl border border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-800">
                    <Icon name={getSkillIcon(skill)} class="size-8" />
                  </div>
                  <span class="text-center text-sm font-medium text-gray-700 dark:text-gray-300">
                    {skill}
                  </span>
                </div>
              ))}
            </div>
          </div>
        );
      })
    }

    <!-- Footer -->
    <div class="mt-20 text-center">
      <p class="text-lg text-gray-600 dark:text-gray-400">
        Always learning and exploring new technologies to stay current with industry trends.
      </p>
    </div>
  </div>
</section>

<script>
  const tablist = document.getElementById('skills-tablist');
  if (tablist) {
    const tabs = tablist.querySelectorAll<HTMLButtonElement>('[role="tab"]');
    const panels = document.querySelectorAll<HTMLElement>('[role="tabpanel"]');

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const target = tab.dataset.category;

        // Update tab states
        tabs.forEach((t) => {
          const isSelected = t.dataset.category === target;
          t.setAttribute('aria-selected', isSelected ? 'true' : 'false');

          if (isSelected) {
            t.classList.add('bg-sky-500', 'text-white', 'shadow-lg', 'dark:bg-sky-600');
            t.classList.remove(
              'bg-gray-100',
              'text-gray-700',
              'hover:bg-gray-200',
              'dark:bg-gray-800',
              'dark:text-gray-300',
              'dark:hover:bg-gray-700',
            );
            // Update badge classes inside the button
            const badge = t.querySelector('span');
            if (badge) {
              badge.classList.add('bg-white/20', 'text-white');
              badge.classList.remove(
                'bg-gray-200',
                'text-gray-600',
                'dark:bg-gray-700',
                'dark:text-gray-400',
              );
            }
          } else {
            t.classList.remove('bg-sky-500', 'text-white', 'shadow-lg', 'dark:bg-sky-600');
            t.classList.add(
              'bg-gray-100',
              'text-gray-700',
              'hover:bg-gray-200',
              'dark:bg-gray-800',
              'dark:text-gray-300',
              'dark:hover:bg-gray-700',
            );
            const badge = t.querySelector('span');
            if (badge) {
              badge.classList.remove('bg-white/20', 'text-white');
              badge.classList.add(
                'bg-gray-200',
                'text-gray-600',
                'dark:bg-gray-700',
                'dark:text-gray-400',
              );
            }
          }
        });

        // Show / hide panels
        panels.forEach((panel) => {
          if (panel.dataset.category === target) {
            panel.classList.remove('hidden');
          } else {
            panel.classList.add('hidden');
          }
        });
      });
    });
  }
</script>
